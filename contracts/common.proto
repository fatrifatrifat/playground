syntax = "proto3";

package quarcc.v1;

message Timestamp {
  int64 seconds = 1;
  int32 nanos = 2;
}

message Money {
  string currency = 1;  // "USD"
  double amount = 2;
}

enum Side {
  UNKNOWN_SIDE = 0;
  BUY = 1;
  SELL = 2;
}

message Empty {}
